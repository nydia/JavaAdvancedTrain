# OpenCode Skills生产代码使用

```shell
# 切到项目目录下面
cd demo\springboot-delay-queue

# 打开opencode
opencode

opencode -p "Parameter 2 of constructor in com.nydia.delay.stream.StreamDelayService required a bean of type 'com.nydia.delay.stream.StreamDelayRocketProducer' that could not be found."
```